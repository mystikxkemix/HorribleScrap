import * as mocha from 'mocha';
import * as chai  from 'chai';
import * as sinon from 'sinon';

import { SERVICE } from '../index';

mocha.describe('SERVICE', function(){
	mocha.it('No tests written yet', function(){
		chai.expect(true).to.be.true;
	});
});